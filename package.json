{
  "name": "deergraph",
  "version": "0.1.0",
  "private": true,
  "description": "DeerGraph monorepo (skeleton). Visual runtime graph for LangGraph agents, extracted from deer-flow per ADR-003.",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "//": "Placeholder root scripts. Real wiring is added when code is migrated."
  }
}
